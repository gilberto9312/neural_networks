[package]
name = "perplexity"
version = "0.1.0"
edition = "2021"

[dependencies]
plotters = "0.3"       # Visualización de comparaciones
rand = "0.8"           # Generación de números aleatorios
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"     # Para cargar dataset JSON
